diff --git a/src/Escape.php b/src/Escape.php
--- a/src/Escape.php
+++ b/src/Escape.php
@@ -25,6 +25,8 @@ class Escape
     /**
      * Escapes for HTML body content (converts to HTML entities)
      *
+     * @psalm-taint-escape html
+     *
      * @param string $value The value to escape
      *
      * @return string The escaped value
@@ -38,6 +40,8 @@ class Escape
     /**
      * Escapes for HTML attribute content (converts to HTML entities and encodes quotes)
      *
+     * @psalm-taint-escape html
+     *
      * @param string $value The value to escape
      *
      * @return string The escaped value
@@ -51,6 +55,8 @@ class Escape
     /**
      * Escapes for URL parameter values
      *
+     * @psalm-taint-escape html
+     *
      * @param string $value The value to escape
      *
      * @return string The escaped value
@@ -64,6 +70,8 @@ class Escape
     /**
      * Escapes for CSS values (strips special characters)
      *
+     * @psalm-taint-escape css
+     *
      * @param string $value The value to escape
      *
      * @return string The escaped value
@@ -77,6 +85,8 @@ class Escape
     /**
      * Escapes for JavaScript string literals
      *
+     * @psalm-taint-escape html
+     *
      * @param string $value The value to escape
      *
      * @return string The escaped value
