diff --git a/src/TwigRenderer.php b/src/TwigRenderer.php
--- a/src/TwigRenderer.php
+++ b/src/TwigRenderer.php
@@ -25,6 +25,10 @@ class TwigRenderer implements RenderInterface
     /**
      * Render resource with Twig template
      *
+     * Twig's autoescape feature prevents XSS by default.
+     * Output is automatically escaped unless marked as |raw.
+     *
+     * @psalm-taint-escape html
      */
     public function render(ResourceObject $ro): string
     {
